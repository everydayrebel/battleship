<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :boards %><br>
    <%= f.text_field :boards %>
  </div>
  <div class="field">
    <%= f.label :player_count %><br>
    <%= f.number_field :player_count %>
  </div>
  <div class="field">
    <%= f.label :player_turn %><br>
    <%= f.number_field :player_turn %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
